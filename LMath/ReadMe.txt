
This program is used to learn Mathematics.

Please run the program for Interface_LMath_with_Knowla\main.py

Note!
in the file of LMath_with_Knowla\OpenMathProblemFolder.py , in line 37,
please make sure that the line is as below based on the system you use

in MAC: - full_directory_separated_by_backslash = temp.split('/')  # split the element by /

in WINDOWS : -  full_directory_separated_by_backslash = temp.split('\\')  # split the element by \\
